**Circular 44px navigation button for stepping through characters (prev/next).** Fills with quiz-blue on hover. Pair two around a counter.

```jsx
<NavButton ariaLabel="Previous">←</NavButton>
<NavButton ariaLabel="Next">→</NavButton>
```
