**Surface panel — every section sits in one.** `radius-2xl`, hairline border, `bg-card`. Pass `interactive` for selectable cards (homepage group cards) to get the hover lift.

```jsx
<Card>…</Card>
<Card interactive onClick={pick}>…</Card>
```
